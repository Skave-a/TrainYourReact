
const useEventListener = (eventName: string, handler: (event: Event) => void, element: EventTarget = window) => {
  // создать слушатель на событие eventName, при его возникновении вызвать handler
  // addEventListener

};

export default useEventListener;
